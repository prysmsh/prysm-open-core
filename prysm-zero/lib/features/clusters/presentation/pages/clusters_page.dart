import 'package:flutter/material.dart';

class ClustersPage extends StatelessWidget {
  const ClustersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Clusters'),
      ),
      body: const Center(
        child: Text('Clusters page - Coming soon'),
      ),
    );
  }
}